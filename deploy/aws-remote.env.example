# AWS Remote Connection Configuration
# For Claude Code or local development connecting to AWS server
#
# Copy to ~/.fractal-aws.env and configure with your values

# AWS EC2 Instance Details
AWS_HOST=your-ec2-public-ip-or-hostname
AWS_USER=ubuntu
AWS_KEY_PATH=~/.ssh/fractal-key.pem

# Project path on remote server
REMOTE_PROJECT_PATH=/home/ubuntu/FractalTrader

# Docker compose file to use
REMOTE_COMPOSE_FILE=docker-compose.aws.yml

# Optional: AWS Region (for aws-cli commands)
AWS_REGION=ap-northeast-1

# Optional: Instance ID (for SSM and monitoring)
AWS_INSTANCE_ID=i-0123456789abcdef0
